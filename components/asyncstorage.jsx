import React, {useEffect} from 'react';
import {View, Text, Button} from 'react-native';
import {writeFile, readFile, deleteFile} from './FileStorage';

const App = () => {
    useEffect(()) => {
        const filename
    }
}